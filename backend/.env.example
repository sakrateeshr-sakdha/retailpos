# Database Connection
DATABASE_URL="postgresql://retailpos_user:StrongPassword123@localhost:5432/retailpos?schema=public&connection_limit=20&pool_timeout=20"

# Authentication & Security
JWT_SECRET="generate-a-strong-random-64-character-secret-key-here"

# Server Configuration
PORT=5000
NODE_ENV=production

# CORS (Comma-separated list of allowed origins, or leave blank to accept same-origin reverse proxy)
CORS_ORIGIN="https://pos.yourgrocerystore.com"
